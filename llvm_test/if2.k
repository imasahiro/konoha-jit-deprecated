using konoha.math.*;
void interSectsSphere(String x, String y) {
    double d = 3.0;

    if (d > 0.0) {
        double t = -30.0 - Math.sqrt(d);
    }
}

void ambient()
{
    int j;
    int ntheta = 8;
    int hitCount = 0;
    for (j = 0; j < ntheta; j++) {
        hitCount++;
    }
    print hitCount;
}
